import 'package:flutter/material.dart';

class InstaFavourite extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Favourite'),
      ),
    );
  }
}
